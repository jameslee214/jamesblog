using Microsoft.UI.Xaml.Controls;

namespace jamesblog.UI.Units
{
    public sealed class MenuListItem : ContentControl
    {
        public MenuListItem()
        {
            this.DefaultStyleKey = typeof(MenuListItem);
        }
    }
}
